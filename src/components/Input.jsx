import React from 'react';
import TextField from '@mui/material/TextField';

const InputArea = () => {
  return (
    <TextField
      label="Input Area"
      variant="outlined"
      fullWidth
      margin="normal"
    />
  );
};

export default InputArea;
